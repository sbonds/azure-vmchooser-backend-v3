apiVersion: v1
kind: Service
metadata:
  name: {{ include "vmchooserbackend.fullname" . }}
  labels:
    {{- include "vmchooserbackend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vmchooserbackend.selectorLabels" . | nindent 4 }}
