apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vmchooserbackend.fullname" . }}-test-connection"
  labels:
{{ include "vmchooserbackend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "vmchooserbackend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
